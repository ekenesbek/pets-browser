# Pets Browser — Agent Configuration
# Copy to .env and fill in your values

# ─── Managed mode (subscription) ─────────────────────────────────────────────
# Agent auth token is generated on install and saved in ~/.pets-browser/agent-credentials.json
PB_API_URL=https://api.petsbrowser.dev/pets-browser/v1
# PB_AGENT_TOKEN=PB1.<agentId>.<agentSecret>
# PB_AGENT_ID=
# PB_AGENT_SECRET=
# PB_AGENT_RECOVERY_CODE=

# ─── BYO mode (bring your own) ──────────────────────────────────────────────
# Proxy provider: decodo | brightdata | iproyal | nodemaven
# Only set these if you have your own proxy account.
# If PB_PROXY_USER/PB_PROXY_PASS are empty, managed mode is used automatically.
# PB_PROXY_PROVIDER=decodo
# PB_PROXY_USER=
# PB_PROXY_PASS=
PB_PROXY_COUNTRY=us
# PB_PROXY_SERVER=          # Full override: http://host:port
# PB_PROXY_SESSION=         # Sticky session port (Decodo: 10001-49999)
# PB_NO_PROXY=1             # Disable proxy entirely (local testing)
# PB_PROFILE=default        # Persistent profile name (set profile:null in code for ephemeral)
# PB_CHROMIUM_NO_SANDBOX=   # 1 force disable / 0 force enable (auto: disabled only in Docker)

# CAPTCHA solving (2captcha.com)
TWOCAPTCHA_KEY=
